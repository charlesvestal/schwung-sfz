[package]
name = "xsynth-shim"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "xsynth_shim"
crate-type = ["staticlib"]

[dependencies]
xsynth-core = { path = "../xsynth/core" }

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
